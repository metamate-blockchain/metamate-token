# metamate-token
Smart Contract for Metamate token

### Installation

yarn install


### Compilation

yarn compile


### Deploy contract

1. Create .env file with same format as .env.example file
2. Run command: npx hardhat script/deploy-token --network mainnet
